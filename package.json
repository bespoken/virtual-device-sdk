{
  "name": "silent-echo-sdk",
  "license": "Apache 2.0",
  "private": false,
  "main": "./lib/src/Index.js",
  "typings": "./lib/src/Index.d.ts",
  "version": "0.2.4",
  "engines": {
    "node": "> 6.0.0"
  },
  "files": [
    "lib/src/*.js",
    "lib/src/*.d.ts"
  ],
  "nyc": {
    "exclude": [
      "lib/test/**"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/bespoken/silent-echo-sdk.git"
  },
  "scripts": {
    "lint": "tslint src/**/*.ts && tslint test/**/*.ts",
    "tsc": "tsc",
    "pretest": "npm run tsc && npm run lint",
    "test": "nyc mocha lib/test/*Test.js",
    "posttest": "nyc report --reporter=json && codecov -f coverage/*.json",
    "typings": "typings install"
  },
  "devDependencies": {
    "@types/chai": "^4.0.0",
    "@types/dotenv": "^4.0.0",
    "@types/mocha": "^2.2.41",
    "@types/sinon": "^2.3.3",
    "chai": "^4.0.2",
    "codecov": "^2.2.0",
    "dotenv": "^4.0.0",
    "mocha": "^3.2.0",
    "nyc": "^10.1.2",
    "sinon": "^2.3.8",
    "ts-node": "^1.7.0",
    "tslint": "^4.0.2",
    "typescript": "^2.4.1"
  }
}
